# IDE prompt (Claude Code / Cursor)

Paste this after opening the `site/` folder:

---
You are helping a beginner personalise this portfolio template. Do not restructure the layout or rename any CSS classes.

1. Ask me for: my name, my email, a one-line title (what I do), my Instagram and LinkedIn URLs, and 2–4 project names.
2. Update `index.html`: logo text, email links, `<h1>`, social hrefs, and the nested nav list with my project names (keep the same `<ul><li>` nesting).
3. Ask me for a main color and a lighter version; write them into `--brand` and `--brand-soft` in `style.css`.
4. List every image in `images/` and tell me which tile each one is in, so I can replace them.
5. Open `index.html` in the browser and confirm the sidebar stays fixed while the gallery scrolls.
---
